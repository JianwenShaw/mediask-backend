# TODO

- Evaluate whether bad-request validation errors should expose specific messages such as `gender must be one of MALE, FEMALE, OTHER` instead of the current generic `invalid parameter`, and define a consistent project-wide policy before changing `GlobalExceptionHandler`.
